@model IEnumerable<TrailLocker.Models.Locker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            UserID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LockerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LockerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LockerID })
        </td>
    </tr>
}

</table>
